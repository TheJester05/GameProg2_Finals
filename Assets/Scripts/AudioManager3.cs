using UnityEngine;

public class AudioManager3 : MonoBehaviour
{
    private static AudioManager3 instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
